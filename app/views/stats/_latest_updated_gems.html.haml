%div.stats.well
  %h2
    = _('Latest updated gems')
  %table.table.table-striped.table-condensed
    %thead
      %tr
        %th
          = _('Gem')
        %th
          = _('Version')
        %th
          = _('Wanted by')
        %th
          = _('Downloads')
    %tbody
      - @popular_gems.each do |gem|
        %tr
          %td= link_to gem.name, rubygem_path(gem.name)
          %td= gem.version
          %td= number_with_delimiter(gem.downloads)
