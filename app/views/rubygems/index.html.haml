%div.page-header
  %h1 All Ruby Gems

%table.table.table-striped.table-condensed
  %thead
    %tr
      %th Name
      %th Gem Version
      %th Downloads
      %th Rpm

  %tbody
    - @gems.each do |gem|
      %tr
        %td= link_to gem.name, rubygem_path(gem.name)
        %td= gem.version
        %td= number_with_delimiter(gem.downloads)
        %td= link_to gem.fedora_rpm.name, fedorarpm_path(gem.fedora_rpm.name) if gem.fedora_rpm != nil

%div.pagination
  = will_paginate @gems